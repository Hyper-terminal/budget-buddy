const Input = ({ ...rest }) => {
  return <input {...rest} className="outline-none border p-3 text-white border-gray-500 bg-gray-600 rounded-lg focus:border-blue-500 transition-all duration-300 ease-linear" />;
};

export default Input;
