const Input = ({ ...rest }) => {
  return <input {...rest} className="outline-none border-2 p-3 border-gray-300 rounded-lg focus:border-blue-500 transition-all duration-300 ease-linear" />;
};

export default Input;
